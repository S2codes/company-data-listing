import React from 'react';

import classes from "../styles/Dashboard.module.css";

const Dashboard = (props) => {
  return (
    <div
    className={classes.dashboard}>Dashboard</div>
  )
}

export default Dashboard